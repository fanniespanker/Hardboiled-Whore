
# CNML Specification v0.5.1 (Draft)

## Foundations

CNML (Contextual Narrative Markup Language) is a constrained XML vocabulary with semantic rules.

All CNML documents MUST be well‑formed XML.

CNML is primarily an experiential and discourse-oriented representation system.

Experiential content refers to represented subjective or interpretive states, not objective truth claims.

CNML encodes semantic, experiential, structural, temporal, prosodic, and spatial relationships, not canonical presentation.

Evaluation, rendering, playback, and projection are interpreter-dependent realizations of CNML source structure.

Operational interpretation MAY derive additional structure for rendering or execution purposes, but such derived structure MUST NOT be treated as intrinsic source semantics.

---

## Core Evaluation Model

CNML evaluation operates across four primary expressive planes:

```text
Narrative × Temporal × Prosodic × Spatial
```

These planes are independently representable and compositional.

## Planes

| Plane     | Meaning                          |
|-----------|----------------------------------|
| Narrative | discourse, cognition, experience |
| Temporal  | musical/performance progression  |
| Prosodic  | affective modulation             |
| Spatial   | typographic/spatial arrangement  |

---

## Narrative Ontology

`<narrative>` does not represent objective reality.

Narrative content is interpreted as a projection generated by a narrator model.

Narrators MAY be:
- unreliable
- dishonest
- hallucinating
- delusional
- temporally inconsistent
- partially reconstructive

CNML discourse is therefore epistemically unstable by default.

---

## GEL Relationship

CNML MAY contain internally persistent ontologically-oriented metadata and semantic structures.

GEL MAY additionally provide external persistent semantic grounding.

Evaluators MAY:
- privilege internal CNML semantics
- privilege external GEL semantics
- merge both systems
- resolve conflicts heuristically

CNML persistence and GEL persistence are therefore orthogonal but interoperable.

---

## Root Element

```xml
<cnml>
```

The root element MAY contain:
- publication-layer frames
- narratives
- metadata
- poems
- music
- discourse content

---

## Publication Layer

Publication-layer frames are optional organizational containers. Beyond the text itself, they may contain various metadata elements.

### Publication Frames

`<series>`
`<story>`
`<book>`

These define publication topology only.

### Publication Metadata Elements

Publication-layer frames SHOULD contain at least one of each of the following:
- `<title>`
- `<author>`
- `<date of="publication">`
- `<identifier scheme="identifier_scheme">`
- `<synopsis>`
- `<license>` — license information, which MUST contain one `<name>` element, and MAY contain any number of each of the following:
  - `<identifier scheme="identifier_scheme">` — an identifier (such as a URI/IRI) for the license; and
  - `<code>` — the legal code of the license; and
  - structured data that represents the terms of the license.
  
Publication frames may also contain any number of genre and audience elements:
- `<genre>`
- `<audience>`

Publication frames may also contain any number of subject elements which indicate the depth of those subjects' presence and/or the author's position (whether moral, academic, or otherwise) regarding those subjects:
- `<references>`
- `<depicts>`
- `<explores>`
- `<examines>`
- `<challenges>`
- `<opposes>`
- `<parodies>`

Publication frames may also embed or reference bibliographic data from other works through the `<external>` element, which contains `<work>` elements.

Examples include:

```xml
<book vocabulary="https://github.com/fanniespanker/Media-Description-Ontology/0.1/ns">

  <title>Hardboiled Whore</title>
  <author>Fannie Spanker</author>
  <date of="publication">2025</date>
  <identifier scheme="IRI">https://fanniespanker.com/armedescorts/hardboiledwhore/</identifier>
  <license>
    <name>Creative Commons Attribution Noncommercial ShareAlike 4.0 International</name>
    <iri>https://creativecommons.org/licenses/by-nc-sa/4.0/legalcode</iri>
  </license>

  <synopsis style="diegetic">
    <heading>Some cops love themselves. Some cops know they’re human garbage.</heading>
  </synopsis>
  
  <genre resource="Fiction.humor/sex_comedy">sex comedy</genre>

  <parodies resource="#Columbo">Columbo</parodies>

  <supports resource="Politics.movements/labor/unions/activism">labor unions in bodily autonomy-oriented feminist activism</supports>

  <audience resource="People.marginalized_groups/GSM">gender and sexual minorities</audience>

  <work 
      about="#Columbo"
      medium="Media.medium/television"
      format="Media.format/series?of=Media.format/movie"
      resource="https://www.imdb.com/title/tt0061496"
  >
    <title>Columbo</title>
    <creator>Richard Levinson</creator>
    <creator>William Link</creator>
    <genre resource="Fiction.crime/mystery/procedural">police procedural mystery</genre>
    
    <character resource="https://id.loc.gov/authorities/sh93004873">
      <name>Columbo</name>
    </character>
  </work>
  
  ...
  
</book>
```

---

## Matter Layer

Matter elements partition reading experience.

```xml
<frontmatter>
<mainmatter>
<backmatter>
```

Matter elements:
- are structural
- are optional
- do not carry frame headers

---

## Narrative Structure Layer

Narrative structure elements organize experiential progression.

```xml
<act>
<chapter>
<scene>
```

### Containment

- `<act>` MAY appear only inside `<mainmatter>`
- `<chapter>` MAY appear only inside `<act>` or `<mainmatter>`
- `<scene>` MAY appear:
  - inside `<chapter>` or `<act>`
  - directly inside `<story>`

All are optional.

### Frame Headers

Zero or one of each of the following:

- `<title>`
- `<synopsis>`
- `<subheading>`

MAY appear once inside:
- `<series>`
- `<story>`
- `<book>`
- `<section>`
- `<act>`
- `<chapter>`
- `<scene>`

---

### Sections

```xml
<section>
```

Sections are general-purpose framing and segmentation units.

#### Allowed Locations

Sections MAY appear inside:
- `<frontmatter>`
- `<backmatter>`

Sections MAY contain zero or one of the following:
- `<title>`
- `<synopsis>`
- `<subheading>`

Sections MAY contain zero or more of the following:
- prose
- narrative content
- metadata

#### Role Attribute

```xml
<section role="">
```

The `role` attribute is optional and evaluator-defined.

Examples:
- dedication
- acknowledgments
- epigraph
- commentary
- notes
- appendix
- index

---

### Narrative Container

All primary experiential discourse SHOULD appear inside:

```xml
<narrative>
```

A narrative MAY contain:
- expression modalities
- prosody
- poems
- music
- nested narrative modalities

---

### Narrative Modalities

#### Narrative Modality Elements

<dream>
<fantasy>
<flashback>
<remember>

are sibling nodes under `<narrative>`, but NOT a single semantic taxonomy.

Instead, they form a 2×2 cognitive operator grid:

---

#### Cognitive Axes

##### Epistemic Source

| Value    | Meaning                        |
|----------|--------------------------------|
| irrealis | generated experiential content |
| memory   | retrieved experiential content |

##### Volition

| Value       | Meaning                  |
|-------------|--------------------------|
| involuntary | non-deliberate emergence |
| voluntary   | deliberate invocation    |

---

##### Mapping

| Element   | Source   | Volition    |
|-----------|----------|-------------|
| dream     | irrealis | involuntary |
| fantasy   | irrealis | voluntary   |
| flashback | memory   | involuntary |
| remember  | memory   | voluntary   |

---

#### Formal Model

NarrativeMode = (EpistemicSource × Volition)

---

### Expression Modalities

```xml
<say>
<shout>
<think>
<feel>
<gesture>
```

#### Semantics

| Element | Meaning                         |
|---------|---------------------------------|
| say     | spoken discourse                |
| shout   | intensified speech              |
| think   | propositional/internal language |
| feel    | autonomic/limbic cognition      |
| gesture | embodied nonverbal expression   |

---

## Prosody

Prosody is a compositional modulation system.

### Dimensions

```xml
<quiet>     <loud>
<calm>      <excited>
<gentle>    <forceful>
<smooth>    <harsh>
<engaged>   <detached>
<emph>      <deemph>
```

Additionally, `<nonverbal>` suppresses implied verbal articulation, forming a virtual 7th dimension.

#### Semantics

Prosody:
- modifies expression quality
- does not alter discourse structure
- composes recursively
- is evaluator-interpreted

---

### Integrity Operators

Integrity operators instruct the evaluator to decorate or alter their text to appear degraded according to aesthetic rules determined by the renderer's configuration. These indicate *diegetic* corruption of representation, whether intentional or unintentional.

These are:

```xml
<redacted>
<censored>
<corrupted>
```

#### Semantics

The `mode` attribute is evaluator-defined and MAY correspond to recognizable corruption aesthetics or encoding artifacts. The `severity` attribute is evaluator defined and MAY correspond to corruption that is more or less intense.

Examples:

`<redacted severity="0.8" source="narrator">Black bars and footnotes abound.</redacted>`
…renders as something like `█████ₐ₄₋₃₁ ████ₙ₁₁₋₂ and █████████ₙ₇₋₁₃ ███████ᵥ₅₋₁.

`<censored severity="0.4" source="government">You wouldn't download an Internet.</censored>`
…renders as something like `You wouldn't [CENSORED] an [CENSORED].`

`<corrupted severity="1.0" mode="mojibake">UTF-8 tastes great.</corrupted>`
…renders as something like: `ॕൔᡆᔭጸ ٴѡࡳѴݥᡳᄠg౲ὥᝡ୴ᐮ`

`<corrupted severity="0.3" mode="zalgo">All hail Zalgo!</corrupted>`
…renders as something like `Ä̙̹͆ll̪̺̝ hail̗̲̍ͧ ͅZaḷ̮͓̈́̆͆g͔͚̳̈͑ͫȍ̯̘̍!͐͗`

---

## Rendering Model

CNML MAY be rendered into:
- HTML
- PDF
- TeX/LaTeX DVI
- SVG
- EPUB
- Markdown
- PostScript
- OpenDocument
- audio (text-to-speech, music)
- interactive runtimes

### Recommended HTML Projection

| CNML            | HTML       |
|-----------------|------------|
| narrative units | `<p>`      |
| prosody         | `<span>`   |
| poem/music      | inline SVG |

---

# Constraints

- CNML documents MUST be well-formed XML
- Inline elements compose recursively
- No cross-boundary inheritance is implied unless evaluator-defined

---

## CiNnaMoN (Cognitive-Narrative Musical Notation DSL)

CiNnaMoN is a structured, human-readable temporal annotation layer for expressive or performative text segments.

Music elements may appear as semantic content inside ANY CNML container.


### CiNnaMoN Semantic Duality Principle

CiNnaMoN defines duration, beat, and measure-length attributes under a **dual semantic regime**:

---

#### 1. Authorial (Normative) Semantics

All temporal attributes (`time`, `beat`, `duration`) primarily serve a **normative compositional function**:

- to guide authors in producing musically coherent notation
- to constrain structural plausibility of rhythmic expression
- to establish local consistency of temporal intent

In this mode:
- values are treated as **intent descriptors**
- authors are NOT expected to perform arithmetic reasoning
- symbolic forms (including fractions) are expressive, not computational

---

#### 2. Operational (Renderer) Semantics

Temporal attributes are secondarily interpreted by renderers as **operational constraints**:

- to validate structural consistency of the score
- to resolve timing relationships for playback, layout, or simulation
- to detect under- or over-specified temporal structures

In this mode:
- values MAY be normalized or evaluated
- interpretation is implementation-defined
- results MUST NOT be re-encoded back into author-facing syntax

Renderers MAY reject temporal systems that cannot be operationally resolved within their target notation or playback model.

---

#### Non-Reification Constraint

Operational interpretation MUST NOT be treated as part of CiNnaMoN source semantics.

That is:

- computed timing is **derivative**, not intrinsic
- renderer resolution does not modify authorial meaning
- CiNnaMoN text remains invariant under evaluation

---

#### Structural Validation Hierarchy

Renderers MAY apply validation at two levels:

1. **Structural plausibility checks**
   - consistency of measure grouping
   - completeness of required fields
   - continuity constraints for tied or sustained events

2. **Temporal resolution**
   - mapping durations into internal time units
   - scheduling of events for output systems

---

#### Key Principle

CiNnaMoN source notation expresses **temporal intent**, not explicit computation.

Authors describe:
- rhythmic structure
- temporal continuity
- expressive grouping
- performative timing relationships

Renderers MAY:
- validate structural consistency
- resolve durations operationally
- map symbolic durations into playback or layout systems

However:

- operational resolution is derivative
- computed timing is not authorial syntax
- renderer interpretation MUST NOT alter source semantics

CiNnaMoN therefore functions simultaneously as:

- a symbolic notation system for human authors
- a temporal constraint system for evaluators and renderers

without collapsing either role into the other.

---

### CiNnaMoN Grammar

The content model is line-oriented and token-based.

#### Core Element

```xml
<music time="" beat="" key="" mode="" tempo="" mood="">
```

##### Attributes
* Together, `time` and `beat` define the enclosing measure duration for operational interpretation purposes:
  * `time`: the number of beats in a measure (numerator of time signature). Example values: 1, 2, 3, 4, 5, 6.5, 3.1415926535, pi, √2.
  * `beat`: the note-value unit representing the beat (inverse duration of the beat). Defines the denominator of the time signature. Example values: 2 = half note, 4 = quarter note, 5 = fifth note, 8 = eighth note, 16 = sixteenth note.
  * Renderers MAY approximate irrational or symbolic temporal values for time and beat.
* `key`: tonal center (A–G with optional accidentals). Examples: `C` for the key of C natural, `Gsharp` for G sharp, `Aflat` for A flat.
* `mode`: modal context (major, minor, lydian, phrygian, dorian, etc.)
* `tempo`: may be descriptive (largo, adagio, moderato, allegretto, etc.) or numeric (real number) in beats per minute (120)
* `mood`: descriptive or desired emotional outcome

---

#### Top-level structure

```
<music> ::= "<music" attributes ">" content "</music>"

attributes ::= (time | beat | key | mode | tempo | mood)*
```

#### Content model

```
content ::= bar ("|" bar)*

bar ::= sequence

sequence ::= element (whitespace element)*
```

#### Element types

```
element ::= note | rest | tie | caesura | speech
```

#### Duration

```
duration ::= ([1-9][0-9]*) "/" ([1-9][0-9]*)   // e.g. 1/4, 3/16, 99/167
```

Examples:
- `7/2`
- `2/3`
- `7/5`
- `3/11`

Durations MAY span multiple measures when interpreted by a renderer as exceeding the enclosing measure duration.

Examples:

```
D:1/4{Two} C:1/4{beats.}

X:2/1{Here are two measures of speech.}

C:7/2{Here are three and a half measures of freely sung vocals.}

R:9/5
```

* Renderers MAY approximate irrational or symbolic temporal values for duration.

---

#### Note syntax

```
note ::= pitch ":" duration "{" lyric "}" 
       | pitch ":" duration
       | pitch "{" lyric "}"
       | pitch

pitch ::= [A-G] accidental? octave?
accidental ::= "#" | "b"
octave ::= -? ( [0] | [1-9][0-9]* )
```

#### Vocalization Semantics

CiNnaMoN note events represent performative pitched articulations.

Lyrics are optional semantic attachments to note events rather than prerequisites for note existence.

An empty lyric body:

```text
{}
```

represents intentionally non-lexical vocalization or unspecified articulation.

Examples include:
- humming
- whistling
- vowel-based singing
- nonverbal melodic performance

Renderers MAY additionally interpret lyricless note events as instrumental or synthesized realizations.

#### Rest syntax

```
rest ::= "R" (":" duration)?
```

#### Tie / sustain syntax

```
tie ::= "~"
```

* Ties connect preceding and following notes or sustain state across elements.

#### Caesura / breath mark syntax

```
caesura ::= "//"
```

* `//` is a **caesura or breath mark**, not a comment.
* CNML does NOT define comments in CiNnaMoN.
* No comment syntax exists.


#### Speech syntax

Speech is a timed vocal articulation primitive. It does not encode pitch; pitch is resolved at evaluation time via renderer configuration, inference, or default policy.

```
speech ::= "X" (":" duration)? "{" lyric "}"
```

* NOTE: Common renderer profiles MAY include syllabic, rhythmic-spoken, or other amelodic vocal realizations.

---

##### Composer-interpreted speech rhythm (structured timing)

Speech MAY be subdivided into multiple articulated (`X`) events for rhythmic articulation.

Example:

```
X:3/8{Here} X:1/8{is} X:1/8{a} X:1/8{phrase.} |
```

Semantics:
- each `X` is a discrete articulation unit
- rhythm is explicitly composed
- timing is fully specified by durations

---

##### Performer-interpreted rhythm (free speech)

Speech MAY be encoded as a single event with internal timing left to interpretation.

Example:

```
X:1/1{Here is a measure of speech.}
```

Semantics:
- `X` defines total temporal window
- internal pacing is performer-determined
- no internal segmentation is specified

---

#### Inheritance Rules (Measure Scope)

Within a given measure (bar):

* The first explicit note MUST define:

  * pitch
  * octave
  * duration
  
* The first explicit rest or explicit speech MUST define:
  
  * duration

* Subsequent `note`s, `rests`, and `speech`es within the same measure MAY omit any of these fields.

  * Missing pitch, octave, or duration values inherit from the most recent explicitly defined values in that measure scope.

* Inheritance resets at each new measure boundary (`|`).

---

# PÂTE (Positional Affine Text Encoding) — v0.4.x Clean Graph Spec (Revised)

## 1. Overview

PÂTE is a deterministic **graph-based spatial constraint language** for CNML Poem DSL.

It defines layout as a set of **directed affine relationships between span anchors**, compiled into a constraint graph and resolved into a geometric layout.

PÂTE is intentionally minimal:
- no solver heuristics
- no energy or convergence modeling
- no recursive width dependency
- no conflicting-constraint resolution system

A PÂTE program is either:
> well-formed (valid constraint graph) or ill-formed (rejected)

---

## 2. Core Model

A PÂTE program defines a directed graph:

```
Graph G = (V, E)
```

Where:

- **V (nodes)** = (span, anchor)
- **E (edges)** = affine constraints

Each edge encodes:

```
(source_span, source_anchor) → (target_span, target_anchor) + δ
```

---

## 3. Spans

A span is an atomic, renderer-defined text unit.

```
Span := immutable rendered unit with fixed width
```

Properties:
- width is externally computed (renderer-defined)
- spans do not depend on other spans for width
- spans expose exactly three anchors:

| Anchor | Meaning |
|--------|--------|
| `<`    | leading edge |
| `|`    | center |
| `>`    | trailing edge |

Anchors are intrinsic properties of spans, not separate objects.

---

## 4. Syntax

### 4.1 Span Expression

```
{{ SOURCE_SPAN_ID? SOURCE_ALIGN_OP TARGET_ALIGN_OP TARGET_SPAN_ID OFFSET? :: CONTENT }}
```

Whitespace is semantically inert.

### 4.2 Key Rule

- All alignment is expressed via operators
- There is no dot notation
- Anchors are not independently addressable in surface syntax

---

## 5. Alignment Operators

Operators define anchor-to-anchor binding:

| Operator | Meaning |
|----------|--------|
| `<` | leading anchor |
| `|` | center anchor |
| `>` | trailing anchor |

An operator pair defines a constraint:

```
SOURCE_ALIGN_OP → TARGET_ALIGN_OP
```

Example:

```
A | > B
```

means:

```
A.center → B.trailing
```

---

## 6. Constraint Semantics

Each expression compiles into:

```
Edge:
  source = (A, anchor_i)
  target = (B, anchor_j)
  offset = δ
```

Meaning:

```
pos(A.anchor_i) = pos(B.anchor_j) + δ
```

---

## 7. Offset System

### 7.1 Syntax

```
OFFSET :=
  SIGN RATIONAL_NUMBER SPAN_WIDTH_REFERENCE
```

```
SIGN := "+" | "-"
RATIONAL_NUMBER :=
  POSITIVE_INTEGER "/" POSITIVE_INTEGER
  | POSITIVE_INTEGER

POSITIVE_INTEGER := [1-9][0-9]*
SPAN_WIDTH_REFERENCE := SPAN_ID
```

Offsets are whitespace-free concatenations:

```
-3/5span2
+1spanA
```

### 7.2 Semantics

Offsets are affine scalars:

```
δ = ± (rational × width(SPAN_ID))
```

Span widths are immutable inputs.

---

## 8. Graph Construction

PÂTE compiles into a directed multigraph:

- nodes = (span, anchor)
- edges = affine constraints

Multiple constraints MAY exist only if they are identical.

---

## 9. Evaluation Model

PÂTE evaluation is deterministic propagation over a static graph:

### Step 1 — Parse
Construct spans and anchors.

### Step 2 — Build Graph
Create edges from alignment expressions.

### Step 3 — Propagate Constraints
Compute positions satisfying:

```
pos(source_anchor) = pos(target_anchor) + δ
```

### Step 4 — Verify Consistency
All constraints must agree exactly.

---

## 10. Validity Rules (STRICT)

A PÂTE program is valid only if:

### 10.1 Unique Constraint Rule

Each anchor has at most one defining constraint.

### 10.2 Consistency Rule

If multiple constraints reference the same anchor:

- they MUST be identical
- otherwise the graph is ill-formed

### 10.3 No Conflict Resolution

PÂTE does NOT:
- resolve contradictions
- prioritize constraints
- approximate solutions

Conflicts are errors.

---

## 11. Anchor Model (Revised + Complete)

Each span exposes **two anchor modalities**:

### 11.1 Implicit Anchors (Intrinsic)

Every span has exactly three implicit geometric anchors:

| Anchor | Meaning       |
|--------|---------------|
| `<`    | leading edge  |
| `|`    | center        |
| `>`    | trailing edge |

Properties:
- derived from span geometry
- not explicitly declared in syntax
- not independently addressable objects
- exist only as projection points in the constraint graph

These anchors are the **only anchors used in alignment operators**.

---

### 11.2 Explicit Anchors (Declared Empty Spans)

Explicit anchors are **syntactic anchor declarations that compile to empty spans**.

They exist only to provide named reference points in the graph.

```
{{ anchor_id }}
```

Semantics:

- an explicit anchor is a span with:
  - zero content
  - zero visual width contribution (or implementation-defined minimal width)
  - full participation in the constraint graph as a node

Formally:

```
ExplicitAnchor ≡ Span(content = ε, width = 0 or minimal)
```

Properties:
- ARE addressable nodes in the graph
- MAY be referenced by SPAN_ID
- behave like spans for constraint resolution
- do not contribute semantic content

---

### 11.3 Relationship Between Explicit and Implicit Anchors

- implicit anchors belong to spans
- explicit anchors are spans themselves
- implicit anchors are NOT addressable objects
- explicit anchors ARE addressable objects

There is no mixing of namespaces:
- implicit anchors: geometric projections
- explicit anchors: graph nodes

---

### 11.4 Key Constraint

Alignment operators ALWAYS operate on:

```
(span, implicit_anchor)
→ (span, implicit_anchor)
```

Explicit anchors are only used as **targets or intermediate nodes**, never as implicit-anchor substitutes.

---

## 12. Alignment Semantics

| Expression | Expansion              |
|------------|------------------------|
| A < B      | A.trailing → B.leading |
| A > B      | A.leading → B.trailing |
| A | B      | A.center → B.center    |
| A <| B     | A.trailing → B.center  |
| A |> B     | A.center → B.trailing  |

Explicit anchors participate only as `B` (span nodes), not as anchor replacements.

Explicit anchors MUST NOT be conflated with implicit anchors.

This enforces:

- no dot notation
- no mixed anchor namespaces
- no “span.anchor” addressing
- no runtime anchor creation

---

## 13. Chaining Model

Multiple constraints form a graph:

```
A |> B
B |> C
```

Produces:

```
A → B → C
```

No special chain syntax is required; chaining is emergent from graph structure.

---

## 14. Evaluation Semantics

PÂTE is NOT a dynamical system.

It is:
> a static constraint graph evaluated by deterministic propagation

No:
- energy model
- epsilon threshold
- iterative convergence semantics
- stochastic relaxation

---

## 15. Determinism

Given identical input graphs:
- output layout is identical
- evaluation order is irrelevant
- no hidden state exists

---

## 16. Canonical IR

All PÂTE programs reduce to:

```
Node = (Span, Anchor)
Edge = (Node → Node, δ)
Graph = Directed Multigraph
```

This is the only semantic representation.

---

## 17. System Positioning

- CNML → structural + narrative frame system
- PÂTE → spatial constraint graph generator
- Poem DSL → evaluation & layout engine
- GEL → external semantic layer

---

## 18. Final Interpretation

PÂTE is:

> a minimal affine constraint language over span anchors, compiling into a static directed graph that deterministically defines layout geometry.

Meaning is entirely encoded in graph structure, not evaluation dynamics.


---
#### Metric Operator

| Operator | Meaning                                                          |
| ~ k      | uniform scaling of internal typographic metric field by scalar k |

Relational form:

```
spanA ~ k spanB
```

means:

> solve for metric field scale of spanA such that `width(spanA) = k × width(spanB)`

Example:
```
{{span1::This is a very long span full of lots of words that go on and on.}}
{{span2<|span1+1/2span1~2span1::A short span.}}
```
... would place the text of span2 1/2 the width of span1 from the center of span1, and stretch that text to fit twice the width of span1.

---